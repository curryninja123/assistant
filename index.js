const static = require('static');         // Static HTML and CSS files
const apps = require('apps');         // Helper JS files
const BrowserWindow = require('BrowserWindow');         // BrowserWindow class
